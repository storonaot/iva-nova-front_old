import styled from 'styled-components'

import { SPACE_2, SECONDARY_FONT } from '../../../../theme'

export const Caption = styled.div`
  margin-top: ${SPACE_2};
  text-align: center;
  font-family: ${SECONDARY_FONT};
`

export const AccentText = styled.div`
  font-weight: 600;
`
